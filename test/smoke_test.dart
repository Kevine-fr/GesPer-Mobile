import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Test minimal — les tests d'intégration sont à ajouter par module.
    expect(1 + 1, equals(2));
  });
}
